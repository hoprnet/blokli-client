# Custom registry image with wget for healthcheck
# Based on registry:2 with busybox wget added for HTTP health probing

FROM registry:2 AS registry
FROM busybox:latest AS busybox

FROM registry:2
COPY --from=busybox /bin/wget /bin/wget
COPY --from=busybox /lib/libc.musl-*.so.1 /lib/
